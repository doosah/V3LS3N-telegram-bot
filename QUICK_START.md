# 🚀 Быстрый старт Telegram планировщика

## Вариант 1: Railway (рекомендуется - самый простой)

### Шаг 1: Подготовка

1. Заполните список ответственных в `server/index.js`:
```javascript
const RESPONSIBLE_PERSONS = {
    'АРХАНГЕЛЬСК_ХАБ_НАХИМОВА': '@username1',
    'МУРМАНСК_ХАБ_ОБЪЕЗДНАЯ': '@username2',
    // ... и т.д. для всех складов
};
```

2. Получите Chat ID:
   - Отправьте боту сообщение в Telegram
   - Выполните в консоли браузера: `getTelegramChatId()`
   - Или откройте: `https://api.telegram.org/bot8241855422:AAG7yW4NT5yoOagAo7My6bXDCdOo-pAhUa8/getUpdates`

### Шаг 2: Деплой на Railway

1. Зарегистрируйтесь на https://railway.app (можно через GitHub)

2. Нажмите "New Project" → "Deploy from GitHub repo"

3. Выберите ваш репозиторий V3LS3N

4. Railway автоматически обнаружит папку `server` и запустит `npm start`

5. Добавьте переменные окружения:
   - Settings → Variables → Add Variable
   - `TELEGRAM_BOT_TOKEN` = `8241855422:AAG7yW4NT5yoOagAo7My6bXDCdOo-pAhUa8`
   - `TELEGRAM_CHAT_ID` = ваш chat_id
   - `SUPABASE_URL` = `https://hpjrjpxctmlttdwqrpvc.supabase.co`
   - `SUPABASE_KEY` = `eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImhwanJqcHhjdG1sdHRkd3FycHZjIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NjIwNzAxMzIsImV4cCI6MjA3NzY0NjEzMn0.jgJD4uKiLoW6MPw5yMrsoYlguowcnn5tl9pKeib7tcs`

6. Готово! Сервер будет работать 24/7

### Шаг 3: Проверка

1. Откройте логи в Railway
2. Проверьте, что сервер запустился
3. Дождитесь времени отправки (07:45, 08:00, 21:45, 22:00 МСК)

## Вариант 2: Render.com (бесплатный тариф)

1. Зарегистрируйтесь на https://render.com
2. Создайте новый Web Service
3. Подключите GitHub репозиторий
4. Укажите:
   - Root Directory: `server`
   - Build Command: `npm install`
   - Start Command: `npm start`
5. Добавьте переменные окружения (как в Railway)
6. Render автоматически запустит сервер

## Вариант 3: GitHub Actions (без отдельного сервера)

1. Добавьте Secrets в GitHub:
   - Settings → Secrets and variables → Actions
   - Добавьте: `TELEGRAM_BOT_TOKEN`, `TELEGRAM_CHAT_ID`, `SUPABASE_URL`, `SUPABASE_KEY`

2. Workflow уже создан в `.github/workflows/telegram-scheduler.yml`

3. GitHub Actions запустит задачи по расписанию

**Примечание:** GitHub Actions бесплатный, но ограничен 2000 минут/месяц

## 📝 Важные замечания

1. **Supabase таблицы:** Убедитесь, что таблицы называются:
   - `operational_reports` (для операционных отчетов)
   - `personnel_reports` (для отчетов по персоналу)

2. **Структура данных:** Таблицы должны содержать:
   - `report_date` (DATE) - в формате YYYY-MM-DD
   - `warehouse` (TEXT) - название склада
   - `shift_type` (TEXT) - 'day' или 'night'
   - `data` (JSONB) - данные отчета

3. **Время в cron:** Используется московское время (Europe/Moscow)

## 🧪 Тестирование локально

```bash
cd server
npm install
node index.js
```

Сервер запустится и будет работать локально, но для работы 24/7 нужен облачный хостинг.

